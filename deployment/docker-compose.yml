version: '3.8'
services:
  PostgreSQL:
    image: PostgreSQL:latest
    restart: unless-stopped
  blackroad-ui:
    build: ../ui
    ports: ["3000:3000"]
  blackroad-api:
    build: ../api
    ports: ["8080:8080"]
